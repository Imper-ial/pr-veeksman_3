<%- include("../partials/header", { title: title }) %>

<h2>Elevoversikt (lærer)</h2>

<% if (students.length === 0) { %>
  <p>Ingen elever registrert ennå.</p>
<% } else { %>
  <table>
    <thead>
      <tr>
        <th>Navn</th>
        <th>E-post</th>
        <th>Telefon</th>
      </tr>
    </thead>
    <tbody>
      <% students.forEach((student) => { %>
        <tr>
          <td><%= student.navn %></td>
          <td><%= student.epost %></td>
          <td><%= student.telefon || "-" %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
<% } %>

<%- include("../partials/footer") %>
